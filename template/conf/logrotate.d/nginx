/data/log/nginx/*.log {
	daily
	missingok
	rotate 60
	compress
	delaycompress
	ifempty
	create 0640 root root
	dateyesterday
	dateext
	dateformat -%Y-%m-%d
	sharedscripts
	postrotate
		[ -s /data/log/nginx/nginx.pid ] && kill -USR1 `cat /data/log/nginx/nginx.pid`
	endscript
}
